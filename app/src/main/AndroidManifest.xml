<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dicoding.picodiploma.submission2bfaa">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <permission
        android:name="com.dicoding.picodiploma.submission2bfaa.READ_DATABASE"
        android:protectionLevel="normal" />
    <permission
        android:name="com.dicoding.picodiploma.submission2bfaa.WRITE_DATABASE"
        android:protectionLevel="normal" />

    <uses-permission android:name="com.dicoding.picodiploma.mynotesapp.READ_DATABASE" />
    <uses-permission android:name="com.dicoding.picodiploma.mynotesapp.WRITE_DATABASE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Submission2BFAA"
        tools:remove="android:appComponentFactory"
        tools:targetApi="p">
        <provider
            android:name=".provider.UserProvider"
            android:authorities="com.dicoding.picodiploma.submission2bfaa"
            android:enabled="true"
            android:exported="true"
            android:readPermission="com.dicoding.mynotesapp.READ_DATABASE"
            android:writePermission="com.dicoding.mynotesapp.WRITE_DATABASE" />

        <activity android:name=".PreferenceScreen" />
        <activity android:name=".FavoriteUser" />
        <activity android:name=".DetailUser" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".AlarmReceiver"
            android:enabled="true"
            android:exported="true" />

        <queries>
            <package android:name="com.dicoding.picodiploma.submission2bfaa" />
        </queries>
    </application>

</manifest>